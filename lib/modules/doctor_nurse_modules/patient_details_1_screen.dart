import 'package:flutter/material.dart';

class PatientDetaiels1Screen extends StatelessWidget {
  const PatientDetaiels1Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
